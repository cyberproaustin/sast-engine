{
  "corpus": "express-credential-in-url",
  "description": "Denied even over TLS, because none of the places a query string ends up are on the network: the access log at both ends, the Referer header of whatever the page loads next, and browser history. The header route carries the same credential where it belongs and is silent, and the pagination route shows that an ordinary caller-supplied value in a URL is not this weakness.",
  "expected": [
    {"cwe": "CWE-598", "sinkFile": "app.js", "sinkLine": 10, "sourceLabel": "req.body.token", "confidence": "high", "note": "a credential built into the query string"}
  ]
}
