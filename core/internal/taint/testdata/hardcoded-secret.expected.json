{
  "corpus": "hardcoded-secret",
  "description": "A key written into the source, where the contents are beside the point: whatever it says, it is in the repository, in every clone of it, and in the history after somebody changes it, which is what makes rotation insufficient on its own. Matching on whether it was written down rather than on what it says is also what makes this precise, because a key read from the environment is not a literal and never matches. Nothing here inspects the string, guesses at entropy, or keeps a list of what a secret looks like. The fourth is in a test module: reported because the key is genuinely in the repository, and not gating because the file does not run in production.",
  "expected": [
    {
      "cwe": "CWE-798",
      "sinkFile": "app.ts",
      "sinkLine": 14,
      "sourceLabel": "\"s3cr3t-dev-key\"",
      "confidence": "high",
      "note": "a signing key written into the call"
    },
    {
      "cwe": "CWE-798",
      "sinkFile": "app.ts",
      "sinkLine": 26,
      "sourceLabel": "\"shared-webhook-secret\"",
      "confidence": "high",
      "note": "an HMAC key written into the call"
    },
    {
      "cwe": "CWE-798",
      "sinkFile": "app.test.ts",
      "sinkLine": 12,
      "sourceLabel": "\"test-signing-key\"",
      "confidence": "high",
      "note": "reported because it is real, and not gating because the module does not run in production"
    }
  ]
}
