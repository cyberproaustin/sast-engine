"""The other module `jobs.views` could mean. Harmless, and equally plausible.

Two candidates rather than one is the whole point: with a single candidate the answer is
a suffix match and this engine makes it, which is what put plane's 397 registrations back
in the surface. With two, the honest answer is silence.
"""
from django.http import HttpResponse


def execute(request):
    return HttpResponse("queued")
